//Метод reverse изменяет порядок элементов в массиве на обратный.
//Метод изменяет исходный массив (он станет перевернутым) 
//и возвращает также перевернутый массив (можно пользоваться и так, и так).
//массив.reverse();


var array = [1,2,3];

console.log(array.reverse());